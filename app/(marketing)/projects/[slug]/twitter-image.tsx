import ProjectDynamicImage, {
  alt,
  contentType,
  size,
} from "@/lib/og/project-dynamic-image";

// fallow-ignore-next-line unused-export
export const runtime = "edge";
export { size, contentType, alt };
export default ProjectDynamicImage;
